'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { ArrowRight, LockKeyhole, UsersRound } from 'lucide-react'
export default function Login(){ const router=useRouter(); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false)
 async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password})});if(r.ok){const next=new URLSearchParams(window.location.search).get('next');router.replace(next||'/');router.refresh()}else{setError('Senha incorreta. Tente novamente.');setLoading(false)}}
 return <main className="login-shell"><section className="login-brand"><div className="brand-mark"><UsersRound/></div><div><p className="eyebrow">DM SYSTEM RECRUIT</p><h1>Pessoas no centro.<br/>Decisões mais inteligentes.</h1><p>Uma experiência simples para gerenciar toda a jornada do seu time.</p></div><aside><span>Privado por padrão</span><strong>Seus dados de RH protegidos em um único lugar.</strong></aside></section><section className="login-panel"><form onSubmit={submit} className="login-card"><span className="login-icon"><LockKeyhole/></span><p className="eyebrow">ACESSO INTERNO</p><h2>Bem-vindo de volta</h2><p>Digite a senha do sistema para continuar.</p><input className="sr-only" type="text" name="username" autoComplete="username" value="dm-system-recruit" readOnly aria-hidden="true" tabIndex={-1}/><label htmlFor="password">Senha</label><input id="password" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required autoFocus placeholder="Sua senha de acesso"/>{error&&<p className="form-error">{error}</p>}<button disabled={loading}>{loading?'Entrando…':'Entrar no sistema'}<ArrowRight size={17}/></button><small>DM System Recruit · Versão 1.0 · Ambiente seguro</small></form></section></main> }
